[project]
name = "perception"
version = "0.1.0"
description = "Robotics slam"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.12.0",
    "datasets>=4.5.0",
    "transformers>=5.0.0",
]

[dependency-groups]
dev = [
    "pre-commit>=4.5.1",
]
