[project]
name = "robotics-algorithms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jax[cuda13]>=0.8.1",
]

[tool.uv.workspace]
members = ["packages/*"]
